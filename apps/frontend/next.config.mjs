

const nextConfig = {
  // Note: for Capacitor static builds, set output: 'export' and
  // convert /track/[code] to /track?code=XXX (query param routing)
  images: { unoptimized: true },
};

export default nextConfig;
